# Database Configuration
# Railway fournira automatiquement DATABASE_URL si vous ajoutez un service PostgreSQL
DATABASE_URL=postgresql://user:password@host:port/database
USE_DATABASE=true

# JWT Secret (générez une clé sécurisée avec: openssl rand -base64 32)
JWT_SECRET=your-super-secret-jwt-key-change-this-in-production

# Admin Password (changez-le en production !)
ADMIN_PASSWORD=your-secure-admin-password

# Node Environment
NODE_ENV=production

# Port (Railway définit automatiquement PORT, mais vous pouvez le spécifier)
PORT=3000

# Next.js Configuration
NEXT_PUBLIC_APP_URL=https://your-app-name.railway.app

